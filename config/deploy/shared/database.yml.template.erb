<%= fetch(:rails_env) %>:
  adapter: postgresql
  encoding: utf8
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  port: 5432
  timeout: 5000
  database: <%= "#{fetch(:application)}_#{fetch(:rails_env)}" %>
